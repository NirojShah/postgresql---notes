
-- Give the custom column name.

SELECT first_name || ' ' ||  last_name AS full_name FROM users3;

SELECT make, model, price, price*0.10 AS discount from cars;


-- as you can see we have used the custom column name.